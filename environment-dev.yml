name: online-alignment-dev
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8,<3.12
  - numpy>=1.20.0
  - numba>=0.56.0
  - scipy>=0.16
  - pip
  - pytest>=7.0.0
  - black>=22.0.0
  - flake8>=4.0.0
  - pip:
      # Install package in editable mode with dev dependencies
      - -e ".[dev]"
